platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: branch-master
outputs:
  - name: mvn-output
  
##run:
#  path: "mvn"
#   args: ["-f", "branch-dev/udm/pom.xml", "package"]

run:
  path: /bin/bash
  args: ["set -e -x mvn -f branch-master/AppDemo/pom.xml test cp branch-master/AppDemo/target/ /home/mvn-output/"]