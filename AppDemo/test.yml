platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: branch-master

run:
  path: "mvn"
  args: ["-f", "branch-master/AppDemo/pom.xml", "test"]