platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest
    
inputs:
  - name: project
outputs:
  - name: mvn-output
  
#run:
#  path: "mvn"
#  args: ["-e","-X","-f", "project/AppDemo/pom.xml", "test"]

#run:
#  path: /bin/bash
#  args: ["set -e -x mvn -f project/AppDemo/pom.xml test cp project/AppDemo/target/ mvn-output/"]

run:
   path: /bin/sh
   args: ["project/test-script.sh"]

#run:
#  path: /bin/bash
 # args: 
 #   - project/output.sh "branch-master"